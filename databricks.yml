# This is a Databricks asset bundle definition for spotify_dab.
bundle:
  name: spotify_dab
  uuid: 89b3b68c-4816-4b2c-86fc-8238a5d44362


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    mode: development
    default: true
    workspace:
      host: http://adb-7405605911696463.3.azuredatabricks.net
    presets:
      source_linked_deployment : false

  prod:
    mode: production
    workspace:
      host: http://adb-7405605911696463.3.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/vavilapallimukunda@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: vavilapallimukunda@gmail.com
        level: CAN_MANAGE
